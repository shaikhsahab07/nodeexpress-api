name: Deployment
on:
  push:
    branches:
      - master
      - dev
env:
  MONGODB_CLUSTER_ADDRESS: cluster0.3hnou.mongodb.net
  MONGODB_DB_NAME: actionsdb01
  PORT: 8080
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      MONGODB_USERNAME: umaisahmed79
      MONGODB_PASSWORD: Proficient@123
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: echo ${{ env.MONGODB_DB_NAME }}
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "{{ env.MONGODB_USERNAME }}"
