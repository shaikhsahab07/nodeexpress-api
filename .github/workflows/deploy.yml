name: Deployment
on:
  push:
    branches:
      - master
      - dev
env:
  MONGODB_CLUSTER_ADDRESS: cluster0.3hnou.mongodb.net
  MONGODB_DB_NAME: actionsdb01
  PORT: 8080
  MONGODB_USERNAME: umaisahmed79
  MONGODB_PASSWORD: Proficient@123
jobs:
  test:
    runs-on: ubuntu-latest
    env:
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Output information
        run: | 
          echo ${{ secrets.MONGODB_CLUSTER_ADDRESS }}
          echo ${{ secrets.MONGODB_PASSWORD }}
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "{{ env.MONGODB_USERNAME }}"
